function tenTime(number) {
  const result = number * 10;
  return result;
  //console.log(result);  not working
}

function cutHuf(number) {
  const huf = number / 2;
  return huf;
}

tenTime(20);
const output = tenTime(20);
console.log(output);
